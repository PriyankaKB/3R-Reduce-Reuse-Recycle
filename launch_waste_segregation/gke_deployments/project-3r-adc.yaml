# ==============================================================================
# KUBERNETES SERVICE ACCOUNT (WORKLOAD IDENTITY / ADC LINK)
# ==============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: project-3r-gke-sa
  namespace: default
  annotations:
    # This annotation maps your pod directly to the secure GCP IAM Service account
    iam.gke.io/gcp-service-account: "project-3r-gke-sa@your-gcp-project-id.iam.gserviceaccount.com"
---
# ==============================================================================
# CONFIGMAP (Environment Targets Only)
# ==============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: project-3r-config
data:
  GCP_PROJECT_ID: "your-gcp-project-id"
  GCS_WASTE_BUCKET: "your-3r-waste-bucket"
  BQ_DATASET: "waste_segregation_3r"
  BQ_TELEMETRY_TABLE: "gke_segregation_telemetry"
  SEGREGATION_AGENT_URL: "http://adk-segregation-service:8080/process"
  ROBOTIC_ARM_AGENT_URL: "http://adk-robotic-service:8081/process"
  HMI_AGENT_URL: "http://adk-hmi-service:8082/process"
  DISPATCH_AGENT_URL: "http://adk-dispatch-service:8083/process"

---
# ==============================================================================
# 1. SEGREGATION AGENT (Port 8080 - GPU Node / Native ADC Engine)
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: segregation-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: segregation-agent
  template:
    metadata:
      labels:
        app: segregation-agent
    spec:
      serviceAccountName: project-3r-ksa # Instructs pod to assume Workload Identity metadata
      nodeSelector:
        cloud.google.com/gke-nodepool: gpu-node-pool # Explicitly pin heavy RAPIDS layers to GPU node pool
      containers:
      - name: agent
        image: us-central1-docker.pkg.dev/your-gcp-project-id/my-docker-repo/segregation-agent:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: project-3r-config
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: segregation-service
spec:
  type: ClusterIP
  selector:
    app: segregation-agent
  ports:
  - port: 8080
    targetPort: 8080

---
# ==============================================================================
# 2. ROBOTIC ARM AGENT (Port 8081)
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: robotic-arm-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: robotic-arm-agent
  template:
    metadata:
      labels:
        app: robotic-arm-agent
    spec:
      serviceAccountName: project-3r-ksa
      containers:
      - name: agent
        image: us-central1-docker.pkg.dev/your-gcp-project-id/my-docker-repo/robotic-arm-agent:latest
        ports:
        - containerPort: 8081
        envFrom:
        - configMapRef:
            name: project-3r-config
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: robotic-arm-service
spec:
  type: ClusterIP
  selector:
    app: robotic-arm-agent
  ports:
  - port: 8081
    targetPort: 8081

---
# ==============================================================================
# 3. SMART HMI AGENT (Port 8082)
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-hmi-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smart-hmi-agent
  template:
    metadata:
      labels:
        app: smart-hmi-agent
    spec:
      serviceAccountName: project-3r-ksa
      containers:
      - name: agent
        image: us-central1-docker.pkg.dev/your-gcp-project-id/my-docker-repo/smart-hmi-agent:latest
        ports:
        - containerPort: 8082
        envFrom:
        - configMapRef:
            name: project-3r-config
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: smart-hmi-service
spec:
  type: ClusterIP
  selector:
    app: smart-hmi-agent
  ports:
  - port: 8082
    targetPort: 8082

---
# ==============================================================================
# 4. REVIEW & DISPATCH AGENT (Port 8083)
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-dispatch-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review-dispatch-agent
  template:
    metadata:
      labels:
        app: review-dispatch-agent
    spec:
      serviceAccountName: project-3r-ksa
      containers:
      - name: agent
        image: us-central1-docker.pkg.dev/your-gcp-project-id/my-docker-repo/review-dispatch-agent:latest
        ports:
        - containerPort: 8083
        envFrom:
        - configMapRef:
            name: project-3r-config
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: review-dispatch-service
spec:
  type: ClusterIP
  selector:
    app: review-dispatch-agent
  ports:
  - port: 8083
    targetPort: 8083

---
# ==============================================================================
# 5. ORCHESTRATOR AGENT (Port 8084)
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adk-orchestrator-deployment
  labels:
    app: adk-orchestrator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adk-orchestrator
  template:
    metadata:
      labels:
        app: adk-orchestrator
    spec:
      containers:
      - name: orchestrator
        image: us-central1-docker.pkg.dev/your-gcp-project-id/my-docker-repo/adk-orchestrator:latest
        ports:
        - containerPort: 8084
        env:
        - name: SEGREGATION_AGENT_URL
          value: "http://adk-segregation-service:8080/process"
---
apiVersion: v1
kind: Service
metadata:
  name: adk-orchestrator-service
spec:
  type: ClusterIP  # Kept private inside the GKE network
  selector:
    app: adk-orchestrator
  ports:
  - protocol: TCP
    port: 8084
    targetPort: 8084

---
# ==============================================================================
# 6. FRONTEND AGENT (Port 3000)
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adk-frontend-deployment
  labels:
    app: adk-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adk-frontend
  template:
    metadata:
      labels:
        app: adk-frontend
    spec:
      containers:
      - name: frontend
        image: us-central1-docker.pkg.dev/your-gcp-project-id/my-docker-repo/adk-frontend:latest
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: adk-frontend-service
spec:
  type: LoadBalancer  # Public UI Gateway Endpoint
  selector:
    app: adk-frontend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000